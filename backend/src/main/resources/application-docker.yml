spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/babilonia
    username: babilonia
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true